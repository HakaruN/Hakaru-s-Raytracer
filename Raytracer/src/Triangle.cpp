//#include "stdafx.h"
/*
#include"Triangle.h"
#include"Vector.h"
#include"Maths.h"


Triangle::Triangle(Vector position, Colour colour, Vector vert1, Vector vert2, Vector vert3) : Renderable(position, colour)
{
	mPosition = position;
	mVertices[0] = vert1;
	mVertices[0] = vert2;
	mVertices[0] = vert3;
}

Colour Triangle::GetColour()
{
	return mColour;
}
Vector Triangle::GetPos()
{
	return mPosition;
}
void Triangle::SetPos(Vector pos)
{
	mPosition = pos;
}
void Triangle::SetColour(Colour colour)
{
	mColour = colour;
}
*/